spring:
  ai:
    bedrock:
      converse:
        chat:
          enabled: true
      cohere:
        embedding:
          enabled: true
app:
  ai:
    chat:
      provider: "aws"
    embedding:
      provider: "aws"
    image-extraction:
      provider: "ollama"
      model: "llava:7b"